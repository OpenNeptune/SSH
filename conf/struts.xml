<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
	<!-- 配置国际化资源文件 -->
	<constant name="struts.custom.i18n.resources" value="i18n"></constant>
	<constant name="struts.enable.DynamicMethodInvocation" value="false" />
	<constant name="struts.devMode" value="true" />
	
	
	<package name="default"  extends="struts-default">
	
	<!-- 定义一个有日志打印的拦截器栈 -->
	<interceptors>
		<interceptor name="log" class="exam.struts.interceptor.LogInterceptor"></interceptor>
		<interceptor-stack name="logstack">
			<interceptor-ref name="log"></interceptor-ref>
			<interceptor-ref name="defaultStack"></interceptor-ref>
		</interceptor-stack>
	</interceptors>
	<default-interceptor-ref name="logstack"></default-interceptor-ref>
	
	
	<action name="configAction" class="configAction">
		<result name="success">/index.jsp</result>
	</action>
	
	</package>
	
	<!-- 包含其他文件 -->
	<!--  
	<include file=""></include>
	-->
</struts>
